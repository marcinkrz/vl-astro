---
import { getT, type Locale } from '@/i18n/messages';
import { getRoute } from '@/i18n/routes';
import LangSwitch from './LangSwitch.astro';
import Button from './_ui/Button.astro';
import Logo from './_ui/Logo.astro';

const locale = (Astro.currentLocale === 'pl' ? 'pl' : 'en') as Locale;
const t = getT(locale);
---
<header class="container">
  <div class="fixed z-[101] top-2 mix-blend-difference">
    <Logo />
  </div>
  <div class="fixed z-[100] top-2 right-2 md:right-4 lg:right-6 w-full flex justify-end gap-x-1">
    <LangSwitch />
    <Button
      href={getRoute(locale, 'contact')}
      variant="primary"
      size="small"
      class=""
    >{t('nav.contact')}</Button>
  </div>
</header>
